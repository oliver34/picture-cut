{
  "name": "photocut",
  "version": "1.0.0",
  "description": "图片裁剪",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "oliver34",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^0.9.0"
  },
  "scripts": {
    "start": "http-server -a 0.0.0.0 -p 8001"
  }
}